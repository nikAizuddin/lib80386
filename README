The functions that are properly documented, are:

    src/io/append_string
    src/io/print_double2string
    src/io/print_int2string

Others are still in document process, not yet ready for release.

ATTENTION!
    Directory changes!

    -> src/io/append_string have been moved to src/string.
    -> others in src/io/* have been moved to src/converter.
    -> src/math/formula_hex2dec have been moved to src/converter,
       and renamed as cvt_hex2dec.